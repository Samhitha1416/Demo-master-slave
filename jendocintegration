pipeline {
    agent any

    tools {
        maven 'M3.9'
    }

    environment {
        DOCKER_IMAGE = "demo-integration"
        DOCKER_TAG = "v14"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build with Maven') {
            steps {
                bat 'mvn -B -DskipTests clean package'
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    echo "🐳 Building Docker image..."
                    bat "docker build -t %DOCKER_IMAGE%:%DOCKER_TAG% ."
                    bat "docker tag %DOCKER_IMAGE%:%DOCKER_TAG% %DOCKER_IMAGE%:latest"
                }
            }
        }

        stage('Run Docker Container') {
            steps {
                script {
                    echo "🚀 Running Docker container (test)..."
                    // Stop and remove old container if exists, then run new one
                    bat '''
                    docker ps -q --filter "name=demo-container" | findstr . && docker stop demo-container && docker rm demo-container || echo "No previous container"
                    docker run -d --name demo-container %DOCKER_IMAGE%:%DOCKER_TAG%
                    docker ps
                    '''
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    echo "📦 Pushing Docker image to Docker Hub..."
                    withCredentials([string(credentialsId: 'dockerhub-pass', variable: 'DOCKER_HUB_PASS')]) {
                        bat '''
                        docker login -u samhitha1610 -p %DOCKER_HUB_PASS%
                        docker push %DOCKER_IMAGE%:%DOCKER_TAG%
                        docker push %DOCKER_IMAGE%:latest
                        docker logout
                        '''
                    }
                }
            }
        }
    }

    post {
        failure {
            echo "❌ Pipeline failed. Check console output for details."
        }
        success {
            echo "✅ Pipeline completed successfully."
        }
    }
}
